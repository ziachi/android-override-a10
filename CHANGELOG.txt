
## Commit #23 — Fix keybox import + add paste XML option
- **Hash:** 0a7c46c
- **Problem:** Keybox import fails silently on non-rooted devices
- **Fix:**
  - Add "Paste Keybox XML" option (bypass file access)
  - Add importKeyboxFromContent() method
  - Add ensureDirectoriesWithLog() with diagnostic logging
  - Add detailed debug logging in import + validate
  - KeyboxFragment: paste dialog, debug logging
- **Files:** OverrideController.java, KeyboxFragment.java, fragment_keybox.xml

## Commit #24 — Rework config storage to /sdcard/.override
- **Hash:** b8816b2
- **Problem:** /data/system/override/ cannot be created without root — mkdirs() fails, all config/keybox operations broken
- **Fix:**
  - Change CONFIG_DIR to /sdcard/.override (no root, no init.rc needed)
  - All sub-paths now under /sdcard/.override/
  - SEPolicy updated (sdcard uses existing sdcard_type rules)
- **Files:** OverrideController.java, override.te, file_contexts

## Commit #25 — Fix cross-process config read (revert to /data/system/override)
- **Hash:** 4b9bfbf
- **Problem:** /sdcard/.override fails cross-process — FUSE blocks GMS from reading (exists=true canRead=false)
- **Root cause:** FUSE per-app file ownership prevents cross-process reads
- **Fix:**
  - Revert CONFIG_DIR to /data/system/override (ext4, no FUSE)
  - init.extras.rc creates dirs at boot (mkdir 0755 system system)
  - Added type_transition in SEPolicy for correct file labels
  - Added makeWorldReadable() — chmod 0644 after every file write
  - Improved ensureDirectories logging
- **Files:** OverrideController.java, override.te, file_contexts

## Commit #26 — Fix SELinux: system_data_file search for /data/system/ navigation
- **Hash:** 74aca0a
- **Problem:** /data/system/override/ exists but all processes see exists=false (ENOENT crash)
- **Root cause:** system_app/priv_app lack "search" on system_data_file:dir — SELinux blocks path traversal through /data/system/
- **Fix:**
  - Added search+getattr on system_data_file:dir for system_app, priv_app, platform_app
  - Added add_name+write for system_app (fallback dir creation)
  - Named type_transition: "override" dir auto-labeled override_data_file
  - Java: importKeybox verifies KEYBOX_DIR exists before write (no more ENOENT crash)
- **Files:** OverrideController.java, override.te
