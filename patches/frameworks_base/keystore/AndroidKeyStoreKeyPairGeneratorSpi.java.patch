diff --git a/keystore/java/android/security/keystore/AndroidKeyStoreKeyPairGeneratorSpi.java b/keystore/java/android/security/keystore/AndroidKeyStoreKeyPairGeneratorSpi.java
index abc12345..def67890 100644
--- a/keystore/java/android/security/keystore/AndroidKeyStoreKeyPairGeneratorSpi.java
+++ b/keystore/java/android/security/keystore/AndroidKeyStoreKeyPairGeneratorSpi.java
@@ -30,6 +30,9 @@ import android.security.KeyStore;
 import android.security.keymaster.KeyCharacteristics;
 import android.security.keymaster.KeymasterCertificateChain;
 
+import com.android.override.KeystoreIntegration;
+import com.android.override.OverrideController;
+
 import java.security.KeyPair;
 import java.security.ProviderException;
 import java.security.cert.Certificate;
@@ -XX,6 +XX,41 @@ public abstract class AndroidKeyStoreKeyPairGeneratorSpi extends KeyPairGenerato
          *
          * IMPORTANT: Locate the section where the attestation certificate chain
          * is retrieved after key generation. This is typically where
          * KeyStore.attestKey() is called or where the cert chain from
          * generateKey() is processed.
          *
-         * Original code retrieves cert chain from Keymaster:
+         * After the original cert chain retrieval, add this hook:
          */
 
+        // === Android Override: Keybox Attestation Hook ===
+        // If Override framework is active with a loaded keybox,
+        // replace the attestation certificate chain with the spoofed one.
+        //
+        // This intercepts AFTER the real key generation (we still need
+        // the key generated in hardware/software keymaster), but REPLACES
+        // the attestation certificate chain with one signed by the
+        // imported keybox.
+        //
+        // Integration point — add after certificate chain retrieval:
+        //
+        //   // Original: List<byte[]> certChain = ... (from Keymaster)
+        //
+        //   if (mSpec.getAttestationChallenge() != null) {
+        //       try {
+        //           List<byte[]> spoofedChain =
+        //               KeystoreIntegration.generateSpoofedAttestationChain(
+        //                   keyPair.getPublic(),
+        //                   mSpec.getAttestationChallenge(),
+        //                   null, // applicationId
+        //                   this instanceof StrongBoxKeyPairGeneratorSpi);
+        //           if (spoofedChain != null && !spoofedChain.isEmpty()) {
+        //               certChain = spoofedChain;
+        //               Log.d("AndroidKeyStore", "Override: attestation chain replaced ("
+        //                   + spoofedChain.size() + " certs)");
+        //           }
+        //       } catch (Exception e) {
+        //           Log.w("AndroidKeyStore", "Override: attestation hook failed", e);
+        //           // Fall through to use real attestation
+        //       }
+        //   }
+        // === End Android Override Hook ===
